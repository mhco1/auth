#! /bin/bash

xhost +local:

sudo docker run \
    -e DISPLAY=$DISPLAY \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v /home:/home \
    -v $HOME/.Xauthority:/root/.Xauthority \
    --network host \
    --rm -it auth-app sh
