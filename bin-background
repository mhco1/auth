#! /bin/bash

xhost +local:

docker run \
    -e DISPLAY=$DISPLAY \
    -e USER=$(whoami) \
    -v /home:/home \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    -v $HOME/.Xauthority:/root/.Xauthority \
    -v $(pwd)/app:/usr/src/app/app \
    -p 5678:5678 \
    --rm -dit auth-app sh
